[workspace]

[package]
name = "bindings"
version = "0.1.0"
edition = "2024"

[lib]
name = "bindings"
crate-type = ["cdylib", "rlib"]

[dependencies]
protocol = { package = "protocol", path = "../protocol" }
brec = { path = "../../../lib/core", features = ["bincode", "csharp"] }
